# include <stdio.h>
int main () {
    int N, sum = 0;
    printf("Enter a number :");
    scanf("%d",&N);
    for(int i=1;i<=N;i++){
        sum+=1;
    }
    printf("sum of natural number upto %d=%d\n",N,sum);
    return 0;
}